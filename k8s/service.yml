apiVersion: v1
kind: Service

metadata:
  name: cicd-demo
  namespace: cicd-demo

spec:
  selector:
    app: cicd-demo

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP